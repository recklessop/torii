"""torii — the gate in front of the MCP estate.

OAuth 2.1 authorization server toward Claude clients, static bearer keys
for everything else, tool-level RBAC with default deny, and an audit
trail. See PRD.md.
"""

__version__ = "0.1.0"
