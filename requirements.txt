# Gateway core
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7

# MCP (server toward clients, client toward upstreams)
mcp>=1.9

# OAuth 2.1 authorization server
authlib>=1.3
httpx>=0.27

# Storage
asyncpg>=0.29
redis>=5.0            # valkey-compatible client

# Credentials
bcrypt>=4.1
cryptography>=42.0   # upstream credential encryption at rest
pyotp>=2.9
segno>=1.6            # inline SVG QR for TOTP enrollment (no external calls)
webauthn>=2.0         # py_webauthn: passkey ceremony verification (Q25)

# Prometheus metrics (FR5 hardening)
prometheus-client>=0.21

# UI
jinja2>=3.1
itsdangerous>=2.1     # starlette SessionMiddleware
python-multipart>=0.0.9
